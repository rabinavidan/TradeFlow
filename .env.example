# Used by `docker compose up` (docker-compose.yml reads .env from the repo
# root automatically). Copy to .env and change JWT_SECRET for anything
# beyond a local demo.
JWT_SECRET=docker-compose-demo-secret-change-me-1234567890
