server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # React Router uses client-side routing (BrowserRouter) — any path
    # that isn't a real static file falls back to index.html so the
    # client-side router can take over, instead of nginx returning 404
    # for a direct visit/refresh on e.g. /trades/123.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
