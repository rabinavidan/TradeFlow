{
  "name": "tradeflow-lite",
  "private": true,
  "version": "1.0.0",
  "description": "TradeFlow Lite - a portfolio-ready full-stack trade-finance workflow app",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:client": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run build --workspace server && npm run build --workspace client",
    "lint": "npm run lint --workspace client && npm run lint --workspace server",
    "typecheck": "npm run typecheck --workspace client && npm run typecheck --workspace server",
    "test": "npm run test --workspace server && npm run test --workspace client",
    "test:e2e": "playwright test --config e2e/playwright.config.ts",
    "test:e2e:allure": "GENERATE_ALLURE=true playwright test --config e2e/playwright.config.ts",
    "allure:generate": "allure generate e2e/allure-results --clean -o e2e/allure-report",
    "allure:open": "allure open e2e/allure-report",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "allure-commandline": "^2.43.0",
    "allure-playwright": "^3.10.2",
    "prettier": "^3.3.3"
  }
}
